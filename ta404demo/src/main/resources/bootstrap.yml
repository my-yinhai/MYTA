spring:
  application:
    name: ta404
  banner:
    location: banner.txt
  output:
    ansi:
      enabled: always
server:
  port: 8081
  servlet:
    session:
      timeout: 7200s
    context-path: /git